model IdempotencyKey {
  id String @id @default(uuid())

  key String @unique

  requestHash String

  method String
  path   String

  response Json?

  status String

  createdAt DateTime @default(now())

  @@index([key])
  @@map("idempotency_keys")
}
